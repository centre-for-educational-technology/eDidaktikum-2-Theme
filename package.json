{
  "scripts": {
    "serve": "cross-env NODE_ENV=development concurrently \"npx tailwindcss -i public/tailwind.css -o public/build/tailwind.css --watch\"  \"live-server ./public\"",
    "development": "cross-env NODE_ENV=development npx tailwindcss -i public/tailwind.css -o public/build/tailwind.css",
    "production": "cross-env NODE_ENV=production npx tailwindcss -i public/tailwind.css -o public/build/tailwind.css",
    "start": "npm run serve"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.0",
    "autoprefixer": "^10.4.0",
    "concurrently": "^5.3.0",
    "cross-env": "^7.0.2",
    "cssnano": "^4.1.10",
    "live-server": "^1.2.1",
    "postcss": "^8.4.5",
    "postcss-cli": "^7.1.1",
    "tailwindcss": "^3.0.7"
  },
  "dependencies": {
    "daisyui": "^1.16.0"
  }
}
