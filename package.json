{
  "scripts": {
    "serve": "cross-env NODE_ENV=development concurrently \"npx tailwindcss -i public/style.css -o public/build/style.css --watch\"  \"live-server ./public\"",
    "development": "cross-env NODE_ENV=development npx tailwindcss -i public/style.css -o public/build/style.css",
    "production": "cross-env NODE_ENV=production npx tailwindcss -i public/style.css -o public/build/style.css",
    "start": "npm run serve"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.2",
    "autoprefixer": "^10.4.4",
    "concurrently": "^5.3.0",
    "cross-env": "^7.0.3",
    "cssnano": "^4.1.11",
    "live-server": "^1.2.1",
    "postcss": "^8.4.12",
    "postcss-cli": "^7.1.2",
    "tailwindcss": "^3.0.24"
  },
  "dependencies": {
    "daisyui": "^2.14.1",
    "flowbite": "^1.4.2"
  }
}
